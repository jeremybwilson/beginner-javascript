const p = document.querySelector('p');
console.log('im in another file');
console.log(`I'm in another file`); // backtick instead of single/double quotes

const age = 100;
console.log(p, age);

/* TYPES In JavaScript
 *
 * Types in JS can be remember using the below...
 *
 * SNOB'N'US
 *
 *
 * String
 * Number    // applies to numbers as well as those with decimal places in them
 * Boolean
 * null
 * Undefined
 * Symbol     // fairly new to JS and provide us with a guaranteed unique identifier
 * Object     // everything in JS is an object
 */
